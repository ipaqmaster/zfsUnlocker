  # List of wireless networks to connect to as colon-delimited SSID:PSK combinations.
  # If var empty/unset, module skips.
  # You can generate network PSKs with the wpa_passphrase command
  # SSID priority is set by the below ordering with the first SSID in the list being
  # the most preferred to connect with.

#wirelessNetworks=("My AP:pskpskpsk" "Another SSID:pskpskpsk")
